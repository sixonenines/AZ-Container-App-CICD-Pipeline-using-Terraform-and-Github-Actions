# Read-only Terraform plan on pull requests that touch infra/.
# Surfaces the proposed change set for review before anything is merged or applied.
# Plans against the dev backend/state; uses the `dev` environment so the same OIDC
# federated credential (subject = environment:dev) is used as the deploy workflows.
name: Terraform plan (PR)

on:
  pull_request:
    paths:
      - 'infra/**'
      - '.github/workflows/terraform-plan.yml'

# Allow only one in-flight plan per PR; newer pushes cancel older runs.
concurrency:
  group: tf-plan-${{ github.ref }}
  cancel-in-progress: true

jobs:
  plan:
    runs-on: ubuntu-latest
    environment: dev
    strategy:
      fail-fast: false
      matrix:
        stack: [platform, app]
    defaults:
      run:
        working-directory: infra/${{ matrix.stack }}
    env:
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      TF_VAR_resource_group_name: ${{ vars.RESOURCE_GROUP_NAME }}
      TF_VAR_environment: dev
      # Only consumed by the app stack; the platform stack ignores undeclared TF_VARs.
      TF_VAR_tf_backend_resource_group_name: ${{ vars.TF_BACKEND_RESOURCE_GROUP }}
      TF_VAR_tf_backend_storage_account_name: ${{ vars.TF_BACKEND_STORAGE_ACCOUNT }}
      TF_VAR_tf_backend_container_name: ${{ vars.TF_BACKEND_CONTAINER }}
    permissions:
      id-token: write # OIDC login to Azure
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v4

      - name: Terraform fmt check
        run: terraform fmt -check -recursive

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="resource_group_name=${{ vars.TF_BACKEND_RESOURCE_GROUP }}" \
            -backend-config="storage_account_name=${{ vars.TF_BACKEND_STORAGE_ACCOUNT }}" \
            -backend-config="container_name=${{ vars.TF_BACKEND_CONTAINER }}" \
            -backend-config="key=${{ matrix.stack }}.dev.terraform.tfstate"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: terraform plan -no-color -var-file="env/dev.tfvars" | tee plan.txt

      - name: Publish plan to job summary
        if: always()
        run: |
          {
            echo "## Terraform plan - \`${{ matrix.stack }}\` (dev)"
            echo '```'
            tail -c 60000 plan.txt 2>/dev/null || echo "no plan output"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
